# 持续供数生产接线计划

目标：在不伪造真人发布字段、不输出凭据的前提下，完成可机器验证的 C3-9 写入链、正式账号导入、启动恢复与调度、全量/增量边界和回滚证据；无法由机器证明的发布红线保持空值并交给真人确认。

## 阶段

- [x] 阶段 1：盘点现有同步、写入、账号导入、启动插件和配置边界
- [x] 阶段 2：闭合 C3-9 staging/manifest/hash/回滚写入链并补验证（单 canary）
- [x] 阶段 3：已知正式账号源 74 条完成 dryRun、正式导入和重复幂等校验；约 233 目标仍缺约 159 条可审计输入
- [x] 阶段 4：恢复路径接入 C3-9 提交 hooks；隔离生产 build readiness 通过，真实中断恢复与调度 tick 仍待窗口验证
- [x] 阶段 5：生成机器证据包、回滚包和真人发布模板
- [ ] 阶段 6：最终复核；若生产红线仍缺真人事实，保持 INTERNAL_READY/HOLD

## 硬边界

- 不输出 `MP_SYNC_AUTH_KEY`、Cookie、`MP_SYNC_ADMIN_TOKEN`。
- 不填写真人姓名、审批时间、真人 verdict 或生产 GO。
- 不在正式写入链未验证前开启全量自动调度。

## 错误记录

| 错误 | 处理 |
|---|---|
| 之前直接构建超时 | 改用 `TEMP/TMP=D:\\tmp` 且延长超时，构建成功 |
| 真实 canary 首次提交发现同 URL 历史路径冲突 | 改为复用 manifest 既有 id/path，重新提交成功 |
| 并行 smoke 共享 SQLite 临时库失败 | 按 `TEMP/TMP=D:\\tmp` 串行重跑，全部通过 |
